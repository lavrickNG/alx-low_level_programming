#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function is the entry point of the program and is called
 * when the program is executed. It uses the 'printf' function to print the
 * message "with proper grammar, but the outcome is a piece of art,", followed
 * by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
