#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function is the entry point of the program and is called
 * when the program is executed. It uses the 'puts' function to print the
 * message "Hello, World!" followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
